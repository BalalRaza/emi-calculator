const styles = (theme) => ({
  container: {
    flexWrap: 'wrap',
    display: 'flex',
  },
  chip: {
    marginTop: theme.spacing(1),
    marginBottom: theme.spacing(1),
  },
  chipLabels: {
    textTransform: 'uppercase',
  },
});

export default styles;
